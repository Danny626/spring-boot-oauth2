server:
  port: 9000

logging:
  level:
    org:
      springframework:
        security: TRACE
        security.oauth2: TRACE

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth2
    username: postgres
    password: asd.123
    driverClassName: org.postgresql.Driver
