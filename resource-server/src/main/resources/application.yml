logging:
  level:
    org:
      springframework:
        security: TRACE
        security.oauth2: TRACE

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
